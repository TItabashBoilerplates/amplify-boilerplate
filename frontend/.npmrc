# Use a flat (hoisted) node_modules like npm/bun. Required for Expo / React Native
# and other tools that don't tolerate pnpm's default isolated symlinked layout.
node-linker=hoisted
# Mirror bun's lenient resolution so the RN/Expo peer graph installs cleanly.
auto-install-peers=true
strict-peer-dependencies=false
